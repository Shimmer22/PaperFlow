I want a local workflow that takes a rough research idea, automatically retrieves papers from multiple scholarly sources, ranks and filters them, deep-reads only the best few, generates standardized briefs, and then critiques whether the idea is genuinely novel or already saturated.

The workflow should be CLI-first, provider-agnostic, and later viewable in a lightweight local desktop UI.

